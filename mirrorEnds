public String mirrorEnds(String string) {
  
  int len = string.length();
  int i = 0;
  String rtnString = new String();
  while (i<len && (string.charAt(i) == string.charAt(len-1-i))){
    rtnString = rtnString.concat(Character.toString((string.charAt(i))));
    i++; 
  } 
  return rtnString;
}
